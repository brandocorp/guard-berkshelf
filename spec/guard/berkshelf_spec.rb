require "spec_helper"

describe Guard::Berkshelf do
  it "has a version number" do
    expect(Guard::Berkshelf::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
